'use strict';

define(['app'], function (app) {

    app.register.controller('demoController', ['$scope', '$http', 'ajaxFactory', 'traslado', function ($scope, $http, ajaxFactory, traslado) {

        $scope.data = traslado.saludo;
        
        console.log($scope.data);

    }]);
});
